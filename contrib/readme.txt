
Copy/clone/symlink dependencies to this dir
-------------------------------------------
	 SDL - For example with: hg clone http://hg.libsdl.org/SDL
	       Note: Currently (2017-09-14) the latest version from mercurial is needed to get working android cmake.
	       Also the debug version of android cmake build does not work unless you apply the included SDL.patch file.
